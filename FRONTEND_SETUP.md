# 🚀 Подключение фронтенда к Supabase

## ✅ Что уже сделано:

1. ✅ `.env` файл создан с вашими credentials
2. ✅ `@supabase/supabase-js` добавлен в package.json
3. ✅ Supabase клиент настроен (`src/lib/supabase.ts`)
4. ✅ `.gitignore` создан (защищает .env от коммита)

---

## 📋 Следующие шаги:

### Шаг 1: Установите зависимости

В терминале выполните:

```bash
npm install
```

Это установит Supabase клиент библиотеку (`@supabase/supabase-js`).

---

### Шаг 2: Запустите приложение

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

---

### Шаг 3: Проверьте подключение к Supabase

Откройте браузер и зайдите на `http://localhost:5173`

Откройте консоль разработчика (F12) и проверьте что нет ошибок связанных с Supabase.

---

## 🔧 Что нужно изменить в коде

Сейчас ваше приложение использует **локальное состояние** (константы из `src/constants.ts`).

Нужно заменить на **Supabase**:

### 1. Authentication (Авторизация)

**Было (старый код):**
```typescript
// src/context/AppContext.tsx
const login = (email: string, password: string) => {
  const user = MOCK_USERS.find(u => u.email === email)
  // ... локальная проверка
}
```

**Станет (с Supabase):**
```typescript
import { supabase } from '../lib/supabase'

const login = async (email: string, password: string) => {
  const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password
  })

  if (error) throw error

  // Получаем профиль пользователя из public.users
  const { data: profile } = await supabase
    .from('users')
    .select('*')
    .eq('id', data.user.id)
    .single()

  setCurrentUser(profile)
}
```

### 2. Получение данных

**Было:**
```typescript
// Данные из константы
const users = MOCK_USERS
```

**Станет:**
```typescript
// Данные из Supabase
const { data: users } = await supabase
  .from('users')
  .select('*')
```

### 3. Создание/Обновление/Удаление

**Было:**
```typescript
// Локальное изменение массива
setUsers([...users, newUser])
```

**Станет:**
```typescript
// Сохранение в Supabase
await supabase
  .from('users')
  .insert([newUser])
```

---

## 📁 Файлы которые нужно обновить:

### Приоритет 1 (Критичные):

1. **`src/context/AppContext.tsx`**
   - Заменить локальную авторизацию на Supabase Auth
   - Подключить Realtime для автообновления данных
   - Использовать `supabase.from()` вместо локальных массивов

2. **`src/App.tsx`**
   - Добавить проверку сессии при загрузке
   - Обработать redirect после авторизации

### Приоритет 2 (Важные):

3. **`src/pages/LoginPage.tsx`**
   - Обновить форму логина для использования Supabase

4. **`src/pages/UsersPage.tsx`**
   - Загружать пользователей из Supabase
   - Обновлять через Supabase API

5. **`src/pages/SkillsPage.tsx`**
   - Загружать навыки из Supabase
   - Сохранять изменения в БД

### Приоритет 3 (Остальные страницы):

6. Все остальные страницы в `src/pages/`

---

## 🎯 Готовые примеры

Я уже создал файл **`src/lib/supabase.ts`** который содержит:

- ✅ Supabase клиент (настроен и готов к использованию)
- ✅ Type definitions для всех сущностей
- ✅ Helper функции для работы с Auth, Database, Storage
- ✅ Realtime subscriptions

**Просто импортируйте и используйте:**

```typescript
import { supabase } from '../lib/supabase'

// Auth
await supabase.auth.signInWithPassword({ email, password })
await supabase.auth.signOut()

// Database
const { data } = await supabase.from('users').select('*')
await supabase.from('users').insert({ ... })
await supabase.from('users').update({ ... }).eq('id', userId)
await supabase.from('users').delete().eq('id', userId)

// Storage
await supabase.storage.from('avatars').upload(path, file)
const { data: url } = supabase.storage.from('avatars').getPublicUrl(path)
```

---

## 🔐 Важно для Google AI Studio

Если вы работаете в **Google AI Studio**, убедитесь что:

1. **Переменные окружения** загружаются правильно
   - Google AI Studio должен поддерживать `.env` файлы
   - Vite использует `import.meta.env.VITE_*` для переменных

2. **CORS настроен** в Supabase
   - Перейдите: Supabase Dashboard → Settings → API
   - В разделе "CORS allowed origins" добавьте:
     - `http://localhost:5173`
     - URL вашего Google AI Studio проекта (если есть)

3. **Сертификаты SSL** работают
   - Supabase использует HTTPS
   - Убедитесь что ваше окружение поддерживает HTTPS запросы

---

## 🐛 Troubleshooting

### Ошибка: "Missing Supabase environment variables"

**Решение:**
1. Убедитесь что `.env` файл находится в корне проекта
2. Перезапустите dev сервер: `npm run dev`
3. Проверьте что переменные начинаются с `VITE_`

### Ошибка: "Failed to fetch" или "Network error"

**Решение:**
1. Проверьте что Supabase проект активен
2. Проверьте CORS настройки в Supabase Dashboard
3. Проверьте что URL правильный: `https://diytvuczpciikzdhldny.supabase.co`

### Ошибка: "Invalid API key"

**Решение:**
1. Проверьте что anon key скопирован правильно (весь JWT токен)
2. Проверьте нет ли лишних пробелов в `.env` файле

---

## 📊 Следующие шаги после установки:

1. ✅ Установить зависимости: `npm install`
2. ✅ Запустить приложение: `npm run dev`
3. ⬜ Обновить AppContext для работы с Supabase
4. ⬜ Протестировать авторизацию
5. ⬜ Обновить страницы для загрузки данных из БД

---

## 💡 Нужна помощь?

Я готов помочь с:
- Обновлением AppContext
- Миграцией страниц на Supabase
- Настройкой Realtime subscriptions
- Загрузкой файлов в Storage
- Любыми другими вопросами

**Просто скажите что нужно сделать дальше!** 🚀
