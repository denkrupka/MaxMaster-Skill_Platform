# TIM.pl — Портал матеріалів для кошторисування

## Що це

React-компонент + проксі-сервер для роботи з каталогом TIM.pl (гуртівня електрики/матеріалів).

**Можливості:**
- 🗂 Дерево категорій з підкатегоріями
- 📦 Перегляд товарів по категоріях (grid / list)
- 🔍 Пошук по SKU
- 📋 Деталі товару: фото, ціна, характеристики, виробник, наявність
- 🧾 Кошторис — додавайте товари, змінюйте кількість, експортуйте CSV
- Без авторизації — використовує публічний GraphQL API TIM.pl

## Архітектура

```
[Ваш портал (React)] → [Проксі-сервер (Node.js)] → [tim.pl/graphql]
```

Проксі потрібен бо браузер блокує прямі запити до tim.pl (CORS).

## Швидкий старт

### 1. Запустити проксі

```bash
cd tim-portal
npm install
node server.js
```

Сервер запуститься на `http://localhost:3001`

### 2. Підключити компонент

Скопіюйте `TIMPortal.jsx` у ваш React-проект:

```jsx
import TIMPortal from './TIMPortal';

function App() {
  return <TIMPortal />;
}
```

### 3. Налаштувати URL проксі

У файлі `TIMPortal.jsx` змініть `PROXY_URL`:

```js
const PROXY_URL = "http://localhost:3001/graphql";
// або ваш production URL:
// const PROXY_URL = "https://your-portal.com/api/tim/graphql";
```

## Файли

| Файл | Опис |
|------|------|
| `server.js` | Проксі-сервер (Express) |
| `TIMPortal.jsx` | React-компонент порталу |
| `package.json` | Залежності для проксі |

## API TIM.pl — що використовується

Всі запити йдуть на `https://www.tim.pl/graphql` через проксі.

| Запит | Опис | Авторизація |
|-------|------|-------------|
| `category` | Дерево категорій | Ні |
| `categoryProducts(id, pageSize, currentPage)` | Товари в категорії | Ні |
| `product(id: "SKU")` | Деталі товару (ціна, фото, характеристики) | Ні* |

\* Ціни на сайті tim.pl показуються публічно. API скоріш за все теж віддає базові ціни без токена.

### Якщо потрібна авторизація (для прайсів/стоків)

```bash
# Встановити токен через API проксі:
curl -X POST http://localhost:3001/login \
  -H "Content-Type: application/json" \
  -d '{"apiUsername":"...", "apiPassword":"...", "webUsername":"...", "webPassword":"..."}'
```

## Інтеграція з вашим порталом

Компонент `TIMPortal` самодостатній. Для інтеграції з кошторисною системою:

1. Підніміть стан кошторису вгору через props/callback
2. Або підпишіться на `estimate.items` через контекст

```jsx
// Приклад з callback:
<TIMPortal onAddToEstimate={(item) => {
  // item = { sku, name, price, qty, manufacturer, image }
  yourEstimateStore.add(item);
}} />
```

## Продакшн

Для продакшну замість окремого Node.js серверу додайте проксі-роут у ваш бекенд:

```nginx
# Nginx приклад:
location /api/tim/ {
    proxy_pass https://www.tim.pl/;
    proxy_set_header Host www.tim.pl;
}
```
